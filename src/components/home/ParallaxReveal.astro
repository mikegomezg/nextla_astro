---
import FeatherMotif from "../shared/FeatherMotif.astro";
---

<section class="imagine-section relative py-24 overflow-hidden bg-black">
    <div class="container mx-auto px-4 relative z-10">
        <div class="text-center mb-24">
            <h2 class="text-5xl md:text-7xl font-bold text-white mb-4">
                Imagine a future...
            </h2>
        </div>

        <!-- Single column layout with one concept per line -->
        <div class="flex flex-col space-y-20 max-w-4xl mx-auto">
            <div class="imagine-item flex items-center">
                <div class="mr-10 hidden md:block">
                    <FeatherMotif
                        width="120"
                        height="120"
                        color="white"
                        opacity="0.2"
                        rotate="0"
                    />
                </div>
                <div>
                    <h3 class="text-3xl md:text-5xl text-white mb-2">
                        when everyone has an audience
                    </h3>
                    <p class="text-xl text-gray-300">
                        In a world where all voices can be heard, the value of connection
                        transcends traditional metrics.
                    </p>
                </div>
            </div>

            <div class="imagine-item flex items-center">
                <div class="mr-10 hidden md:block">
                    <FeatherMotif
                        width="120"
                        height="120"
                        color="jade"
                        opacity="0.3"
                        rotate="15"
                    />
                </div>
                <div>
                    <h3 class="text-3xl md:text-5xl text-white mb-2">
                        when connection replaces consumption
                    </h3>
                    <p class="text-xl text-gray-300">
                        Moving beyond passive experiences to create meaningful exchanges
                        that enrich both humans and AI.
                    </p>
                </div>
            </div>

            <div class="imagine-item flex items-center">
                <div class="mr-10 hidden md:block">
                    <FeatherMotif
                        width="120"
                        height="120"
                        color="teal"
                        opacity="0.3"
                        rotate="-15"
                    />
                </div>
                <div>
                    <h3 class="text-3xl md:text-5xl text-jade font-medium mb-2">
                        when the only limit is attention
                    </h3>
                    <p class="text-xl text-gray-300">
                        Attention becomes the most valuable resource in an economy where
                        infinite possibilities compete for finite focus.
                    </p>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .imagine-item {
        animation: fade-in-up 0.8s ease-out forwards;
        animation-delay: calc(var(--index, 0) * 200ms);
        opacity: 0;
    }

    .imagine-item:nth-child(1) {
        --index: 1;
    }

    .imagine-item:nth-child(2) {
        --index: 2;
    }

    .imagine-item:nth-child(3) {
        --index: 3;
    }
</style>

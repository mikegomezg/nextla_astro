---
/* ————————————————————————————————
   IMPORT + DEBUG
   ———————————————————————————————— */
// Either A (string via ?url)      ↓
import cosmicBg from "../assets/visuals/cosmic_vector.svg?url";

// Or B (object → .src)            ↓
// import cosmicAsset from "../assets/visuals/cosmic_vector.svg";
// const cosmicBg: string = (cosmicAsset as any).src ?? cosmicAsset;

/* Tiny sanity check — remove later */
console.info("Hero background URL:", cosmicBg);
---

<section
    id="hero"
    class="relative flex items-center justify-center overflow-hidden
         bg-center bg-cover"
    style={`background-image:url('${cosmicBg}');min-height:90vh;max-height:110vh;`}
>
    <!-- headline with its own overlay -->
    <div class="relative px-6">
        <h1
            class="relative z-10 text-12xl sm:text-6xl md:text-7xl font-extrabold
                    text-white text-center"
        >
            Imagine a balanced world
        </h1>

        <!-- overlay only the width of the text block -->
        <span
            aria-hidden="true"
            class="absolute inset-0 rounded-md bg-background/10
                     backdrop-blur-sm -z-0 pointer-events-none"
        ></span>
    </div>
</section>

---
import { Image } from "astro:assets";
import featherImg from "../assets/visuals/feather.png";
---

<section
  class="flex flex-col overflow-hidden pt-32 pb-24 px-6 bg-background relative items-center justify-start"
>
  <!-- Background Glow -->
  <div
    class="absolute inset-0 z-0 bg-gradient-radial from-featherbg via-featherbg/40 to-transparent opacity-80 blur-3xl pointer-events-none"
  ></div>

  <!-- Feather Image (in normal flow) -->
  <div class="relative flex justify-center items-start w-full">
    <Image
      src={featherImg}
      alt="Feather"
      width={800}
      height={1000}
      class="relative z-10 mx-auto h-auto max-w-[60vw] object-contain"
      loading="eager"
    />
  </div>

  <!-- Text Cards -->
  <div
    class="relative z-20 grid grid-cols-1 md:grid-cols-2 gap-10 max-w-5xl mt-24 text-text"
  >
    <div class="p-6 bg-surface/70 backdrop-blur-md rounded-xl shadow-lg">
      <h3 class="text-highlight text-lg font-bold mb-2">
        What is Nexla?
      </h3>
      <p>
        Nexla is a platform for managing creative attention — the one thing that can't be automated.
      </p>
    </div>

    <div class="p-6 bg-surface/70 backdrop-blur-md rounded-xl shadow-lg">
      <h3 class="text-highlight text-lg font-bold mb-2">
        What does the name mean?
      </h3>
      <p>
        Nexla comes from the Aztec word for "tribute." It represents the power and scarcity of creative insight.
      </p>
    </div>
  </div>
</section>

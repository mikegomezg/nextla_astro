---
import { Image } from "astro:assets";
import featherImg from "../assets/visuals/feather-upscale.png";
---

<section class="flex flex-col items-center text-center bg-background">
  <h2 class="text-highlight text-3xl md:text-4xl font-bold m-8">
    What is Nexla?
  </h2>
</section>

<section class="flex flex-col md:flex-row items-center justify-center bg-background relative gap-4 md:gap-6 px-4">
  
  <!-- Left Card -->
  <div class="z-20 p-6 bg-surface/70 backdrop-blur-md rounded-xl shadow-lg w-[240px] min-h-[320px] flex flex-col justify-center text-center">
    <h3 class="text-highlight text-lg font-bold mb-2">
      About the Platform
    </h3>
    <p class="text-text">
      Nexla is a platform for managing creative attention — the one thing that can't be automated.
    </p>
  </div>

  <!-- Feather Image -->
  <div class="relative z-10 mx-4">
    <img
      src={featherImg.src}
      alt="Feather"
      width="600"
      height="900"
      class="h-auto max-h-[85vh] object-contain"
      loading="eager"
      decoding="async"
    />
  </div>

  <!-- Right Card -->
  <div class="z-20 p-6 bg-surface/70 backdrop-blur-md rounded-xl shadow-lg w-[240px] min-h-[320px] flex flex-col justify-center text-center">
    <h3 class="text-highlight text-lg font-bold mb-2">
      About the Name
    </h3>
    <p class="text-text">
      "Nexla" derives from the Aztec word for tribute — symbolizing the value and scarcity of true creative insight.
    </p>
  </div>

</section>

<section class="w-full px-0">
  <div class="h-[4px] w-full bg-highlight bg-repeat-x bg-center bg-[length:100px_4px]"></div>
</section>

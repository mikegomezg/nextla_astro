    ---
    import logo from "../assets/logos/logo.svg";
    import ThemeToggle from "../components/ThemeToggle.astro";
    ---

    <header
        class="flex z-50 h-20 w-full px-6 bg-black items-center justify-between fixed top-0 md:h-24"
    >
        <div class="flex items-center text-2xl md:text-3xl">
            <!-- 2) Now h‑[1em] = exactly your current font-size (2xl on mobile, 3xl on desktop) -->
            <!-- <img
                src={logo.src}
                alt=""
                aria-hidden="true"
                class="h-[1em] w-auto"
                width="32"
                height="32"
            /> -->
            <!-- 3) Gap between icon & text -->
            <span class="ml-1 font-black text-highlight tracking-[.04em]">Nextla</span>
        </div>
        <!-- Right side: Theme Toggle + Mobile Menu -->
        <div class="flex items-center">
            <!-- Theme Toggle Button -->
            <ThemeToggle />
            <!-- Mobile Menu Toggle -->
            <!-- <button
                id="menu-toggle"
                aria-label="Toggle navigation menu"
                class="p-2 ml-2 text-white focus:outline-none md:hidden"
            >
                <svg
                    xmlns="http://www.w3.org/2000/svg"
                    class="h-6 w-6"
                    fill="none"
                    viewBox="0 0 24 24"
                    stroke="currentColor"
                >
                    <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M4 6h16M4 12h16M4 18h16"></path>
                </svg>
            </button> -->
        </div>

        <!-- Mobile Navigation -->
        <!-- Center: Navigation -->
        <nav
            class="hidden md:flex absolute left-1/2 transform -translate-x-1/2 select-none
                space-x-2 md:space-x-3 lg:space-x-6 xl:space-x-8"
        >
            <a
                href="/#"
                class="text-base font-semibold text-white hover:text-highlight tracking-wide md:text-lg"
                >Home</a
            >
            <a
                href="/#feather"
                class="text-base font-semibold text-white hover:text-highlight tracking-wide md:text-lg"
                >About</a
            >
            <a
                href="/#characters"
                class="text-base font-semibold text-white hover:text-highlight tracking-wide md:text-lg"
                >Characters</a
            >
            <a
            href="/scene"
            class="text-base font-semibold text-white hover:text-highlight tracking-wide md:text-lg"
            >
            Read
            </a>
            <a
                href="/#cta"
                class="text-base font-semibold text-white hover:text-highlight tracking-wide md:text-lg"
                >Contact</a
            >
        </nav>
    </header>

    <!-- Fixed Accent Line Under Header -->
    <div class="fixed top-20 md:top-24 w-full h-[4px] bg-highlight z-40"></div>
